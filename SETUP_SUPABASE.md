# خطوات ربط المشروع بـ Supabase

## 1. إنشاء الجداول على Supabase

روح على Supabase Dashboard بتاعك:
👉 https://supabase.com/dashboard/project/oufpkiwpnzjcbkojlirj

### الطريقة السهلة:

1. اضغط على **SQL Editor** من القائمة الجانبية
2. اضغط على **New Query**
3. انسخ كل محتوى ملف `supabase_tables.sql` والصقه في الـ Editor
4. اضغط على **Run** أو اضغط `Ctrl+Enter`

✅ كده الجداول اتعملت بنجاح!

---

## 2. التحقق من الجداول

افتح **Table Editor** من القائمة الجانبية، لازم تلاقي:

- ✅ **volunteers** - جدول المتطوعين
- ✅ **tickets** - جدول الشكاوى
- ✅ **ideas** - جدول الأفكار

---

## 3. اختبار الفورم

1. افتح الموقع من هنا: https://[YOUR-REPLIT-URL]
2. انزل لقسم "تعالى نتكلم" تحت
3. جرّب تبعت:
   - **انضم لينا** - نموذج التطوع
   - **شكوى** - نموذج الشكاوى
   - **فكرة** - نموذج الأفكار

4. لو ظهرت رسالة نجاح ✅ معناها الفورم اشتغل صح!

---

## 4. شوف البيانات في لوحة التحكم

1. افتح `/admin` في المتصفح
2. كلمة المرور: `13572468852`
3. روح على تاب **البيانات** فوق
4. هتلاقي كل البيانات اللي دخلت من الفورم

---

## الكود اللي بيُرسل للـ Supabase:

### مثال: نموذج التطوع
```javascript
await supabase
  .from('volunteers')
  .insert([{
    name: 'اسم المتطوع',
    phone: '01234567890',
    district: 'العرب',
    area: 'الإعلام والسوشيال ميديا',
    created_at: new Date().toISOString()
  }])
```

### مثال: نموذج الشكاوى
```javascript
await supabase
  .from('tickets')
  .insert([{
    name: 'اسم المشتكي',
    district: 'الشرق',
    problem_type: 'الإسكان',
    details: 'تفاصيل الشكوى...',
    ticket_number: 'T12345678',
    status: 'pending',
    created_at: new Date().toISOString()
  }])
```

### مثال: نموذج الأفكار
```javascript
await supabase
  .from('ideas')
  .insert([{
    name: 'اسم صاحب الفكرة',
    idea: 'الفكرة...',
    implementation: 'طريقة التنفيذ...',
    created_at: new Date().toISOString()
  }])
```

---

## ملاحظات مهمة:

✅ **Row Level Security (RLS)** مُفعّل على كل الجداول
✅ السماح للجميع بالإضافة (Public Insert)
✅ السماح بالقراءة للمستخدمين المصادق عليهم
✅ رقم الشكوى يتم توليده تلقائياً بصيغة `T12345678`

---

## مشاكل محتملة وحلولها:

### ❌ "حدث خطأ، حاول مرة تانية"

**السبب**: الجداول مش موجودة على Supabase

**الحل**: نفذ الـ SQL من ملف `supabase_tables.sql`

---

### ❌ الفورم بيبعت بس البيانات مش ظاهرة في الأدمن

**السبب**: RLS Policies ممكن تكون محتاجة تعديل

**الحل**: تأكد إن الـ Policies موجودة في SQL Editor

---

## 🎉 تمام! المشروع جاهز

كل حاجة شغالة دلوقتي:
- ✅ الفورم بيبعت للـ Supabase مباشرة
- ✅ صفحة الأدمن بتجيب كل البيانات
- ✅ Footer فيه "made with addvalues.tech"
- ✅ المشروع جاهز للنشر

---

**للنشر على Replit:**
اضغط على زر **Deploy** من Replit Dashboard
