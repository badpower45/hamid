[build]
  command = "npm run build"
  publish = "build"

# SPA redirect: serve index.html for all paths (useful for client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
